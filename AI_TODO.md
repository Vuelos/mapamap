when expanding maps with connections or neightboors without them, they desync and broke, I want the map to fill empty spaces but keep the existing ones as they are, abstracting from the maps sizes and connections on the editor
create inventory like on factorio, show it to the left of pallete picker and blueprints components(move them to the right with same size and position, one can be active at the time)
blueprints are saved to inventory instead of current blueprints bar
tiles, objects, warps can be places on inventory
show the warps with a circle like on map_editor mod and let me edit its destination map and cordinates graphically letting me pick map and showing it with the cursor to set destination.
handle control Z control Y for blueprints